# 시각검증 체크리스트 — 역할별 (2026-07-07)

배포: PR#1 머지 → master@ab75785 (R1~R4 리메디에이션 21커밋). 스키마/마이그 변경 없음.

## ⚠️ 검증 환경 = 로컬 dev (운영 아님)
- **운영 DB는 청정화되어 데이터 없음 → 문서 렌더 검증 불가.** 반드시 로컬에서.
- 로컬 `.env` = dev Supabase(`dlyblffixqltchwcleil`, 시드 데이터 보유). dev 안전모드(outboundGuard)로 외부발송(공단 이메일) 자동 차단 → 생성/제출까지 안심.
- 로컬 코드 = master@ab75785 (배포분 동일).
```
! npm run dev        # http://localhost:3000, dev DB
# 데이터 부족 시 재시드: npx tsx --env-file=.env scripts/seed-all.mts
```
- 계정: 워커 `01070000001`/`worker1234!` · 매니저 `manager01~03`/`Manager1234!` · 운영자 `admin`/`admin1234!`

## 0. 데이터 준비 상태 — ✅ 준비 완료 (2026-07-07, seed-doc-verify.mts 실행)
`scripts/seed-doc-verify.mts` 실행으로 5종 전부 실내용 렌더 가능. (재실행 시 해당 배정·기간만 재생성=비파괴)
```
npx tsx --env-file=.env scripts/seed-doc-verify.mts
```
**테스트 레시피:**
| 워커 | 로그인 | 현장 | 케이스 | 훈련생 |
|---|---|---|---|---|
| 강도윤 | `01070000000`/`worker1234!` | 햇살 카페 | **1:1** | 정해성 |
| 서아린 | `01070000001`/`worker1234!` | 햇살 마트 | **1:多** | 문가은, 김도하 |

**기간 지정 (문서 생성 시):** `2026-06-01 ~ 2026-06-12`
- FIELD 구간 06-01~06-05 → 출근부·훈련일지·훈련생 종합평가
- ADAPTATION 구간 06-08~06-12 → 적응지도 일지·종합평가
- 검증됨: 두 워커 모두 5종 전부 ✓ (출근10·PRE/FIELD·ADAPT 일지·확정 평가 2종)

- ENDED 딥링크(R4-3) 테스트용: `다기관경력`(01077777777) asg#21·#22 ENDED 존재(내용은 빔, UI 확인용).

---

# 1. 워커(직무지도원)로 로그인 — `01070000001` / `worker1234!`

### 1-A. 홈·출퇴근·일지 (회귀 스모크)
- [ ] 홈 로드 정상: 출퇴근 카드·알림·"놓친 업무"·멀티현장 스위처
- [ ] 출근/퇴근 버튼 정상 동작
- [ ] 일지 단건 저장 정상
- [ ] 일지 **일괄(소급) 저장** 정상 → 저장 후 안내 메시지 확인 (R4-7: 기간 밖/미재적 날짜는 "저장 안 됨"으로 명시)

### 1-B. 문서 미리보기 — PDF 5종 렌더링 (★핵심)
문서 메뉴 → 종류·훈련생·기간 선택 → **미리보기**. 5종 각각:
**출근부 / 지원고용 훈련일지 / 훈련생 종합평가 / 적응지도 일지 / 적응지도 종합평가**
- [ ] 레이아웃·폰트(HCR 한글) 안 깨짐, 표 칸 안 밀림·안 잘림
- [ ] 서명란 위치 정확 (직무지도원/사업체담당자, 빈칸이어야 할 곳은 빈칸)
- [ ] 훈련생명·현장명·기간이 화면 선택값과 일치
- [ ] 1:1 vs 1:多 표기 정확 (그날 훈련생 2명+ = 1:多) — 출근부·일지
- [ ] 휴일 표시 정확 (공휴일 + 현장 커스텀 휴무)

### 1-C. 문서 제출 (3경로 비교용 — B단계)
- [ ] 위 문서를 **제출** → 문서조회에서 다시 열기 = 공단 발송 스냅샷(`buildDocPayload`). 1-B 미리보기와 **동일해야 함**.

### 1-D. R4 수정분 (눈으로 확인)
- [ ] **R4-9**: 훈련생 문서 미리보기에서 훈련생 **미선택** → "훈련생을 선택해주세요" 안내 (빈 PDF 안 나옴)
- [ ] **R4-3**: **종료(ENDED)된 배정**의 수정요청 딥링크로 문서 페이지 진입 → 카드·제출버튼 **정상 표시** (예전엔 빈 화면)
- [ ] **R4-4**: 배정 **근무기간이 지난 뒤** 출근 시도 → **차단**되는지 (가능하면)
- [ ] **R4-6**: 계약서 링크에서 **서명** → 서명 후 배정이 이상한 근무형태로 안 바뀌는지 (겹침 시 기존 슬롯 유지)

---

# 2. 매니저로 로그인 — `manager01~03` / `Manager1234!`

### 2-A. 콘솔 회귀 스모크
- [ ] 대시보드 로드 정상 (상단 카드 숫자)
- [ ] 출근부 관리·일지관리 로드 정상
- [ ] 문서조회에서 **워커 제출본 PDF 열람** 정상 (3경로 비교의 "제출본")
- [ ] 알림/공지 "모두 읽음" 눌렀을 때 뱃지 정상 소멸

### 2-B. 배정·계약 (R4 이중배정 방지)
- [ ] **R4-5**: 이미 다른 현장에 **수락(ACCEPTED)** 상태인 워커를 다른 현장에 **직접 배정** 시도 → **차단**(이중배정 방지)
- [ ] **R4-10**: **같은 현장** 갱신/재계약 계약 발행 → 잘못된 "시간 겹침 409" 안 뜨는지 (오탐 제거)
- [ ] 계약 발행 → 워커 서명 → 매니저 문서 서명(sign) 흐름 정상

---

# 3. 운영자(admin)로 로그인 — `admin` / `admin1234!`

### 3-A. 문서 생성 (3경로 비교의 "admin 생성본")
- [ ] admin 콘솔 → 문서 생성/조회 → PDF 생성. **1-B 워커 미리보기와 동일해야 함** (payload drift 확인)
- [ ] 훈련생 문서 미선택 시 400 안내 (헬퍼 통일 확인)

### 3-B. 급여 (R4-1 과지급 방지 ★)
- [ ] 급여 계산 실행 → 명세 숫자 확인. 특히 **소급 일지(batch-save)만 있고 출퇴근 시각 없는 날이 근무일로 안 잡히는지** (DAILY 일수·MONTHLY 근무일수 과다 없어야 함)

### 3-C. 접속기록 (R4-8)
- [ ] 감사서류 패키지(audit-package) ZIP 다운로드 → **접속기록(AccessLog)에 기록 남는지**
- [ ] 제출문서 ZIP 다운로드·제출본 PDF 열람 → 접속기록 남는지
- [ ] 공단 발송(문서 send) → 워커별 접속기록 남는지

---

# 4. 교차 확인 — ★3경로 PDF 출력 일치 (payload drift)
같은 문서(같은 훈련생·기간)를 아래 3경로로 뽑아 **나란히 비교**:
1. [ ] 워커 미리보기 (1-B)
2. [ ] 워커 제출본 = 공단 스냅샷 (1-C)
3. [ ] 운영자 생성본 (3-A)

→ 레이아웃·값·서명·휴일까지 **셋이 동일**하면 payload 3사본 일관성 OK. 다르면 그 지점이 drift 버그(기록·공유).
→ **통과 시** 보류된 "문서 payload 6파일 일원화 리팩터" 착수 여부 재논의 가능.

---

# 5. 배포 인프라 (역할 무관, 지금 바로 가능)
- [ ] Vercel 대시보드에서 이번 배포(ab75785) **빌드 성공(초록)** 확인 — 실패 시 운영은 직전 버전 유지 (데이터와 무관하게 확인 가능)
