# AbleLink 남은 작업 To-Do (2026-07-19, 4차 재감사 종료 시점)

> 📌 2026-07-20 추가: **PDF 서명부 페이지 분할(출근부 2·3분할)·종합평가 소견 장문 붕괴 전량 수정·배포 완료**(`3792360`·`1aa4773`·`61bdfc7`). 상세 = `docs/WORK_STATUS_2026_07_20_PDF_SIGNATURE.md`. 이 문서의 잔여 목록에는 영향 없음.

4라운드 전수 재감사(8병렬×4) + 대량 수정 후 **미처리로 남긴 항목** 종합. 배포된 것은 제외.
**라이브 위험(P0/P1/악용 P2)은 0.** 아래는 전부 노무사 대기·정책 판단·심층방어(비악용)·사업 액션·백로그.

> ✅ 4차 재감사 완료(8병렬, HEAD 64f4431): **신규 P0/P1/P2 0.** 3차 신규코드(BirthDateSelect·traineeSiteGuard·contracts effectiveTo·claim/락) 회귀 0 확증. 신규는 전부 P3(아래 §I·§J에 반영).

---

## A. 노무사 회신 대기 (유일한 급여 잔여)
- [ ] **월급제(MONTHLY) 휴일근로 기본급 100% 누락** — `lib/payroll/computeRun.ts` MONTHLY 분기. 공휴일이 proRateDaySet 제외+schedDays 상한이라 기본급 미반영, 가산 0.5배만. 월급제 휴일가산율 해석에 여지 → **노무사 확인 후 조치**. HOURLY/DAILY는 정상.

## B. 정책 판단 필요 (결정 주시면 착수)
- [ ] **연차 USE 소진 순서(FIFO)** — 현재 발생순 소진. 만기임박 우선이 워커에 유리(월개근분은 발생순=만기순이라 실차 작음). 노무사 문의 시 병행 가능.
- [ ] **연차 EXPIRE 재발화 불가** — 소멸 1회성이라 소멸일 이후 ADJUST(+)분이 재소멸 안 됨. 저빈도 엣지.
- [ ] **로그인 레이트리밋 정책 통일** — admin/manager는 IP 단일키(worker는 IP+계정 2중). 분산 스프레이 대비 계정축 예산 추가 여부.
- [ ] **rateLimit fail-open** — Upstash 장애 시 인메모리 폴백(서버리스 무력). 의도적 가용성 트레이드오프. **운영 Upstash env 설정 실측 필요**(설정돼야 브루트포스 방어 성립).
- [x] **매니저 PDF/공단발송 플랜게이트** — **STANDARD 유지 확정(2026-07-19 사용자 결정).** 코드 변경 없음. 유료(STANDARD+) 프리미엄 기능으로 확정.
- [ ] **정책 관찰 2건** — gov-status 수동 SUBMITTED 표시에 서명게이트 없음(PDF 미송출 상태플래그) · document-versions GET이 sourceData(서명 data-URI) 전문을 STARTER도 열람(DOC_INBOX 티어 정합이나 필요최소 초과). 티어 정책 재확인.

## C. 심층방어·일관성 (비악용, 선택)
- [ ] **worker/site/current·worker/logs/missing 훈련생 목록 기관 스코프** — docs/context엔 적용했으나 이 둘은 미적용(표시 전용, 불변식상 비악용). nested 쿼리 재구성이 회귀 위험이라 3차에서 수용. 마켓/현장병합이 불변식 깨면 동일 클래스 노출.
- [ ] **PII 접속기록 리스트류** — 민감PII(장애·계좌·주민번호 등) 대량조회는 완비. 성명+연락처만 노출하는 리스트(admin/workers·worker-accounts 등)는 제8조 임계 미달로 추가 불요 판정(참고).
- [ ] **context 이탈 훈련생 마감서류** — 피커는 현재 재적(currentSiteId)만, 가드는 placement 기간겹침. 지난달 이동 훈련생 마감서류가 드롭다운 미표시(fail-closed·UX 협소).

## D. 위생 (저가치, 선택)
- [ ] **json 무가드 → 500(400 기대) ~56곳** — try 안이라 크래시 없음, cosmetic. 공개/워커 라우트 우선.
- [ ] **날짜 verbatim 저장 경미 2곳** — worker/holidays(String date 컬럼), bulk-generate(Date.UTC 롤오버). MAX_RANGE 상한. 데이터 위생.
- [ ] **BigInt 딥테일 잔여** — 요청유래 미가드는 종결. 세션/DB유래·이미가드 제외.

## E. TOCTOU·경합 잔여 (극저 실현성)
- [ ] **manager-signup 동명 Agency 동시 생성** — 서로 다른 신청이 같은 agencyName 동시 approve 시 P2002→500(재시도로 자연해소).
- [ ] **admin holiday PATCH vs worker DELETE-accept 경합** — 정확히 겹치면 P2025→500. 극저.
- [ ] **계약 발행 dedup 10초** — 순차 더블클릭 방어 한정. 진짜 ms동시성엔 이중 알림톡 가능(락/유니크 없는 best-effort).
- [ ] **checkQuota 소프트리밋 락 밖** — FREE 인원/현장 한도 1~2 초과 가능(정원·급여 무관).
- [ ] **holiday-requests 중복 PENDING** — advisory 락으로 완화. partial unique 마이그 미포팅(규율상 마이그 전용).

## F. 사업·운영 액션 (코드 아님)
- [ ] 카카오 비밀번호 재설정 알림톡 템플릿 등록(`KAKAO_RESET_PW_TEMPLATE_CODE`)
- [ ] 통신판매업 신고 → 포트원(본인인증·계좌 예금주 조회) 재개
- [ ] 운영 Upstash(rateLimit) env 설정 실측
- [ ] staging 환경 구축 · 스토리지 dev/prod 격리
- [ ] 운영 DB 재셋업(초기화됨 — seed 또는 수동 셋업)

## G. 백로그 (의도적 보류)
- [ ] 대시보드 Phase3 · 급여명세서 고도화 · 2층 타입드헬퍼
- [ ] 급여 계산 큐/작업테이블 전환(경보 시) · cron 교차월 캐치업 공백
- [ ] HOURLY payLines 반올림 미세 드리프트(그리드 재저장 시 수 원 이동)

## H. ②500 저장오류 (사용자 보고) — E2E로 유력원인 해소 확인
- [x] **dev E2E 12/13 통과**(2026-07-19): 매니저 로그인·워커목록·**임시비번 발급 PATCH 200+비번노출**(그때 500나던 경로)·생년월일 저장·급여기준 등록·날짜라우트 3종(review 400·missing 폴백·audit-package 400)·연차목록 전부 정상. → 보고된 저장 500의 유력원인(401→500·날짜) 해소 확인.
- [ ] **[신규 P3] workers PATCH birthDate format-only 검증** — `app/api/admin/workers/[id]/route.ts:83`이 `/^\d{4}-\d{2}-\d{2}$/`만 봐서 `1975-13-40` 통과(E2E에서 400 기대인데 200). **String 컬럼 verbatim 저장이라 500·크래시 없음**(데이터 위생). 다른 날짜검증과 통일하려면 `isValidYmd`로 교체.

## K. PoC 직무지도원 체험 모드 (신규 워크스트림 — 설계 논의 중)
- [ ] **설계 확정 후 착수** — 상세: `docs/POC_TRIAL_MODE_DESIGN_2026_07_19.md`. 요지: 실운영에서 사전세팅 없이 직무지도원이 혼자 전 흐름 체험(자가 프로비저닝·격리 체험기관·사업체만 디지털사인·위탁기관담당자는 PDF 수기란·공단발송 하드블록). **열린 결정 2건**: ①체험자 범위(불특정 다수 vs 파일럿 소수) ②PDF 수기란 적용범위(체험만 vs 전체).

## I. 4차 재감사 신규 — 날짜 왕복검증 형제갭 3곳 ✅ 수정 완료(미배포)
format-only regex만 있고 `isValidYmd` 미적용 → month 13·비ymd 입력이 Invalid Date → DateTime Prisma 필터에서 500나던 것 차단. 각 라우트 기존 의미론 보존(review·audit-package=400, missing=현재월 폴백). tsc0·vitest365 무회귀.
- [x] `app/api/admin/audit-package/route.ts` — periodStart/End `isValidYmd` 왕복검증(400)
- [x] `app/api/admin/review/route.ts` — yearMonth 가드에 `|| !isValidYmd(\`${yearMonth}-01\`)`(400)
- [x] `app/api/admin/document-runs/missing/route.ts` — ym 가드에 동일(현재월 폴백)

## J. 4차 재감사 신규 — 연차·계약 P3 ✅ 수정 완료(미배포, N+1 제외)
- [x] **계약 동일월 재생성 타이브레이크** — `admin/payroll/contracts` GET·computeRun orderBy에 2차 정렬 `{id:"desc"}` 추가(최근 생성분 결정적 선택). 고유 effectiveFrom 무영향·골든 무회귀.
- [x] **연차 DELETE 워커락** — `admin/leave/[workerId]` DELETE를 `withWorkerAssignmentLock`으로 감쌈(cron EXPIRE 재계산과 상호배제).
- [x] **연차 FIFO 만료임박 우선** — `lib/leave/accrual.ts` grants 정렬을 expiresAt 오름차순(null 후순위)→발생일→id로 변경. 동일 만료일은 발생일순 유지라 기존 테스트 26/26 불변, 무만료 ADJUST(+)만 후순위로 밀려 만기손실 방지.
- [x] **연차 리스트 accrued 집계 비대칭** — `admin/leave` GET `accrued`에 ADJUST 순액 반영(accrued−used−expired−paidOut=balance 항등). 양수 조정은 기존과 동일.
- [ ] **traineeSiteGuard N+1** — generate당 site.findUnique(인덱스 PK). **의도적 보류**: 제거하려면 가드 시그니처를 전 호출부에 스레딩해야 해 회귀 표면 대비 실익 없음. 순수 성능·정합 무관.
