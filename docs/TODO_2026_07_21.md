# AbleLink 남은 작업 To-Do (2026-07-21, 8축 전수감사 + 결제 심화 종료 시점)

> 이 문서는 `docs/TODO_2026_07_19.md`(4차 재감사 롤업)를 잇는 최신 종합본입니다.
> 07-20 PDF 서명부·07-21 8축 전수감사·07-21 결제 심화감사에서 **종결된 항목은 제거**하고, 남은 것만 정리했습니다.

**라이브 위험(P0 / P1 / 악용 가능 P2) = 0.** 아래는 전부 노무사 대기 · 정책 판단 · 사업 액션 · 심층방어(비악용) · 백로그입니다.

---

## 07-20~21 종결 (참고 — 조치 완료)

- **결제·구독·환불 전량** — P1(billing 선환불 순서 역전+레이트리밋)·P2(charge benign 오판 이중과금)·P3(FREE 한도 복원·cancel 감사로그 선기록·구독페이지 PLAN_PRICES 단일화)·보류1(claim TTL 재산정, `CLAIM_FRESH_MS`=1h+멱등키 회전)·보류2(refundKind 판별자)·보류3(해지 메시지 정합). 커밋 `77e82e2`(혼합)·`a53d9cc`(refundKind 단독). **push·배포·운영 마이그(20260721140000) 적용·프로덕션 실측 완료.**
- **크로스테넌트 훈련생 PII 6곳** — worker/site/current·logs/missing·home/[workerId]·trainee-report·admin/logs·workers 목록. (07-19 §C 첫 항목 해소)
- **워커 날짜 왕복검증·AI음성 RL·docs toEmail 검증·연차 ADJUST 락·퇴근 재확인 60분·일지 800자 상한** 등 8축 감사 P2 전량.
- **성능** — 계약목록 select·backup 상한·income-tax data 제외·PDF 폰트 tracing 정정.
- **PDF 서명부/평가소견 장문 붕괴**(07-20, `3792360`·`1aa4773`·`61bdfc7`).

---

## A. 노무사 회신 대기 — 유일한 급여 잔여
- [ ] **월급제(MONTHLY) 휴일근로 기본급 100% 누락** — `lib/payroll/computeRun.ts` MONTHLY 분기. 공휴일이 proRateDaySet 제외+schedDays 상한이라 기본급 미반영·가산 0.5배만. 월급제 휴일가산 해석 여지 → **노무사 확인 후 조치**. HOURLY/DAILY 정상.

## B. 정책 판단 필요 (결정 주시면 착수)
- [ ] **연차 EXPIRE 재발화 불가** — 소멸 1회성, 소멸일 이후 ADJUST(+)분 재소멸 안 됨. 저빈도 엣지.
- [ ] **로그인 레이트리밋 정책 통일** — admin/manager는 IP 단일키(worker는 IP+계정 2중). 분산 스프레이 대비 계정축 예산 추가 여부.
- [ ] **정책 관찰 2건** — gov-status 수동 SUBMITTED 표시에 서명게이트 없음(PDF 미송출 상태플래그) · document-versions GET이 sourceData(서명 data-URI) 전문을 STARTER도 열람(DOC_INBOX 티어 정합이나 필요최소 초과). 티어 정책 재확인.
- [ ] **document-versions / surveys 응답 페이로드 트림** (8축 감사 잔여) — sourceData·formSnapshot 전문 응답을 소비처 확인 후 축소.

## C. 사업·운영 액션 (코드 아님)
- [ ] **운영 Upstash(rateLimit) env 설정 실측** — 미설정 시 인메모리 폴백=서버리스 무력(브루트포스 방어 성립 안 함). **유일하게 보안 실효 있는 인프라 항목 — 우선순위 높음.**
- [ ] 카카오 비밀번호 재설정 알림톡 템플릿 등록(`KAKAO_RESET_PW_TEMPLATE_CODE`)
- [ ] 통신판매업 신고 → 포트원(본인인증·계좌 예금주 조회) 재개
- [ ] 토스 부분환불 가맹점 재심사 신청 (부분환불 배포 완료 상태 근거)
- [ ] staging 환경 구축 · 스토리지 dev/prod 격리
- [ ] 운영 DB 재셋업(초기화됨 — seed 또는 수동 셋업)

## D. 심층방어·위생 (비악용, 선택)
- [ ] **context 이탈 훈련생 마감서류** — 피커는 현재 재적(currentSiteId)만, 가드는 placement 기간겹침. 지난달 이동 훈련생 마감서류가 드롭다운 미표시(fail-closed·UX 협소).
- [ ] **workers PATCH birthDate `isValidYmd` 미적용** — `app/api/admin/workers/[id]/route.ts`가 `1975-13-40` 통과(String 컬럼 verbatim 저장이라 500·크래시 없음). 다른 날짜검증과 통일용 P3.
- [ ] **json 무가드 → 500(400 기대) ~56곳** — try 안이라 크래시 없음, cosmetic. 공개/워커 라우트 우선.
- [ ] **날짜 verbatim 저장 경미 2곳** — worker/holidays(String date), bulk-generate(Date.UTC 롤오버).
- [ ] **BigInt 딥테일 잔여** — 요청유래 미가드는 종결. 세션/DB유래·이미가드 제외.
- [ ] **폰트 서브셋** (8축 감사 잔여) — HCR 4종 tracing 정정 후에도 번들 큼. 글리프 시각검증 필요·별도 트랙.

## E. TOCTOU·경합 (극저 실현성, 대부분 의도적)
- [ ] **checkQuota 소프트리밋 락 밖** — FREE 인원/현장 한도 1~2 초과 가능(정원·급여 무관). **의도적.**
- [ ] **계약 발행 dedup 10초** — 순차 더블클릭 방어 한정. 진짜 ms동시성엔 이중 알림톡 가능(best-effort). **의도적.**
- [ ] **traineeSiteGuard N+1** — generate당 site.findUnique(PK 인덱스). **의도적 보류**(가드 시그니처 전 호출부 스레딩 필요, 실익 없음).
- [ ] manager-signup 동명 Agency 동시 생성 → P2002→500(재시도 자연해소)
- [ ] admin holiday PATCH vs worker DELETE-accept 경합 → P2025→500(극저)
- [ ] holiday-requests 중복 PENDING — advisory 락 완화, partial unique 마이그 미포팅(규율상 마이그 전용)

## F. 백로그 (의도적 보류)
- [ ] 대시보드 Phase3 · 급여명세서 고도화 · 2층 타입드헬퍼
- [ ] 급여 계산 큐/작업테이블 전환(경보 시) · cron 교차월 캐치업 공백
- [ ] HOURLY payLines 반올림 미세 드리프트(그리드 재저장 시 수 원 이동)

## G. 신규 워크스트림 (설계 논의 중·미착수)
- [ ] **PoC 직무지도원 체험 모드** — `docs/POC_TRIAL_MODE_DESIGN_2026_07_19.md`. 자가 프로비저닝·격리 체험기관·사업체만 디지털사인·위탁기관담당자 PDF 수기란·공단발송 하드블록. **열린 결정 2건**: ①체험자 범위(불특정 다수 vs 파일럿 소수) ②PDF 수기란 적용범위(체험만 vs 전체).

## H. 별도 관리
- [ ] 직종 확대(요양보호사·활동지원사 등) — 별개 직역, 별도 트랙(`todo_profession_expansion_separate`)

---

## 참고 (조치 불요)
- `__tests__/worker.session.test.ts`(JWT) 병렬 부하 시 간헐 타임아웃(5s 초과). 격리 실행 8/8 통과 — **부하성 플레이크, 회귀 아님.** 필요 시 `testTimeout` 상향으로 별건 정리 가능.

## 실질 우선순위
1. **노무사 회신**(A — 유일한 급여 미결)
2. **운영 Upstash env 설정**(C — 유일하게 보안 실효 있는 인프라 항목)
3. **정책 결정 3~4건**(B — 결정만 주시면 코드는 소량)
4. 나머지는 위생·백로그·의도적 보류 — 급하지 않음
