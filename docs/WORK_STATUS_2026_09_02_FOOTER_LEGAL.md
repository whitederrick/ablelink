# 작업 상태 — 공개 푸터 법정표시 갱신 (2026-09-02)

able-link.co.kr 공개 푸터의 고객센터 번호 변경 + 통신판매업 신고번호 표기. **커밋 2건 push·프로덕션 배포·운영 실측 완료. 이 건 잔여 0.**

## 요구사항 (사용자)
- 고객센터 `070-4300-7141`로 변경.
- `통신판매업 제2026-서울금천-1798호` 추가, **위치는 사업자등록번호 다음**.

## 변경 내용

### 1) 값 갱신 — `713890b`
`lib/businessInfo.ts` 단일 출처 2줄만 수정. 이 파일이 공개 랜딩·약관·개인정보처리방침·환불정책·요금안내 푸터 전체의 유일한 출처라 한 곳 수정으로 전 화면 반영.

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `phone` | `010-8484-7141` | `070-4300-7141` |
| `mailOrderNo` | `null` (푸터 자동 숨김) | `"제2026-서울금천-1798호"` |

- 전화번호가 소스에 하드코딩된 곳은 이 파일 한 곳뿐임을 전수 확인(`backups/*.json` 제외).
- `mailOrderNo`를 게이트·분기 조건으로 쓰는 로직 0건(`grep mailOrderNo` 결과 = 타입 선언·값·행 생성 3곳뿐) → null 해제 부작용 없음.

### 2) 줄 배치 수정 — `965e76b`
`components/LegalFooter.tsx`의 `INFO_LINE_GROUPS`에서 통신판매업 신고번호를 **사업자등록번호와 분리해 단독 줄**로.

```diff
-  ["사업자등록번호", "통신판매업 신고번호"],
+  ["사업자등록번호"],
+  ["통신판매업 신고번호"],
```

- **왜**: 1차 배포(`713890b`) 후 운영 실측에서, 두 항목이 같은 줄이면 텍스트 폭이 **371.5px**로 360px 폰의 컨테이너(320px)를 초과 → `통신판매업 신고번` / `호 제2026-…` 처럼 **어절 중간에서 쪼개짐**. 데스크톱(컨테이너 384px)에서는 한 줄에 들어가 육안으로 안 드러남.
- **수정 후**: 해당 줄 폭 **218.1px** → 가장 좁은 320px 폰(컨테이너 280px)에서도 줄바꿈 0.

## 최종 푸터 구성
```
상호 플랫포레스트(Platforest) · 대표자 곽은하
사업자등록번호 197-86-02010
통신판매업 신고번호 제2026-서울금천-1798호
주소 서울특별시 금천구 디지털로 130, 13층 1309호 (가산동, 남성프라자)
고객센터 070-4300-7141 · 이메일 platforest.inc@gmail.com
개인정보 보호책임자 곽은하(대표)
호스팅 제공 Vercel Inc.
```

## 검증 (운영 able-link.co.kr 실측)
- tsc 0 · vitest 478/478 (양 커밋 각각).
- 구 번호 `010-8484-7141` 운영 화면 잔존 **0건**. 신 번호·신고번호 표시 확인.
- 필드 순서: 사업자등록번호 → 통신판매업 신고번호 (SSR HTML 노드 순서 실측).
- **줄바꿈 실측**(Range API로 줄 분할 추출): 360px 폰(컨테이너 320px)·320px 폰(컨테이너 280px) 양쪽 모두 통신판매업 줄 1줄 유지·어절 쪼개짐 없음.
- 브라우저 스크린샷 시각검증 완료.

## 알려진 잔여(이번 변경과 무관, 기존부터 존재)
- 320px 폰에서 상호 줄이 `곽은` / `하`로 쪼개짐. 360px 이상에서는 정상. iPhone SE 1세대급 폭이라 실사용 비중 낮아 미수정.
- 주소·이메일 줄은 좁은 폭에서 2줄로 접히나 **어절 경계에서 끊겨** 가독성 문제 없음.

## 후속 연계
통신판매업 신고 완료 → `[[verification_portone_2026_06_18]]`(신분·통장 인증 / 포트원)에서 "통신판매업 신고 후 재개"로 보류해 둔 건의 선행 조건이 해소됨. 재개 여부는 사업 판단.

## 교훈
**공개 푸터는 데스크톱 폭에서 한 줄에 들어가도 모바일에서 어절이 쪼개진다.** 푸터 항목을 한 줄에 묶을 때는 데스크톱 컨테이너(384px)가 아니라 **320px 폰 기준 컨테이너(280px)**로 텍스트 폭을 검산할 것. 워커·사업체 화면은 모바일 우선·고령 가독성이 기준.
