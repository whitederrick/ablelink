# AbleLink 남은 작업 To-Do (2026-08-11)

> `docs/TODO_2026_07_21.md`를 잇는 최신 종합본. 오늘 종결된 항목은 §0에 근거와 함께 남기고,
> 남은 것만 §A~ 이하에 정리했다.
>
> **★2026-08-22 갱신 — §C(파일럿)은 운영 마이그·배포까지 전량 종결.** §B(D-1)도 구현 완료.
> 파일럿 기준 문서는 `docs/PILOT_ROLLBACK_INSTRUCTION_2026_08_13.md`(v5-a).
> ★`PILOT_DESIGN_2026_08_12_v1_8.md`(인앱 격리)는 **폐기된 설계**다 — 참조하지 말 것.

**라이브 위험(P0/P1/악용 가능 P2) = 0. 미푸시 0. 미배포 0. 운영 마이그 미적용 0.**

---

## 0-1. 2026-08-22 종결 — 파일럿 실사용에서 나온 문서 결함 4건 (전부 push·배포 완료)

| 커밋 | 내용 |
|---|---|
| `3ab7833` | **출근부에 휴무일 반영** — 등록된 `SiteHoliday`(countAsWorkday=false) 날짜를 문서에서 제외 |
| `477db01` | **6주 달 서명부 밀림** — 여백만 13mm 압축해 한 장에(표 형태·글자크기 불변) |
| `4a50eb4` | **일지 측정시간 = 직무지도원 입력값** · **수행정도 미입력 기본**(체크박스) + 마이그 `20260822130000` |
| `c8211a9` | **출근부 빈 칸에 '휴무' 표기** — 커스텀 휴무일 + 평일 공휴일 |
| `f141909` | **'주말' 표기 + 시간 괄호 제거** — 출근부·일지 공통 |
| `f228d02` | 일지 괄호를 **수행정도 라벨이 있을 때만** 두름 |
| `6e670b8` | **파일럿 서비스 단계(지원고용/적응지도)** 추가 + 문서 목록 단계별 분리 |

### ★★출근부 표기 규칙 (2026-08-23 확정) — **빈 서식 = 기록 누락 신호**

| 날 | 총 지도시간 | 1:多 |
|---|---|---|
| 근무일 | `12:30 / ~18:00 / 5.5h` | `5.5h` |
| 커스텀 휴무일 · 평일 공휴일 | **`휴무`** | `-` |
| 토·일 무근무 | **`주말`** | `-` |
| **사유 없는 평일 공백** | 원본 빈 서식 `: / ~ : / (h)` | `(h)` |

- ★주말이 공휴일보다 우선(토요일 공휴일은 `주말`) · 근무 기록이 있으면 사유 무시하고 시각
- ★★**빈 서식이 남아 있으면 그것이 곧 '기록 누락' 신호**다 — 이 설계 의도를 모르면
  "빈칸도 `-` 로 채우자"로 정리해 버릴 수 있다. **지우지 말 것.**
- 시간에 괄호를 두르지 않는다(일지는 수행정도 라벨이 있을 때도). 빈 서식의 `(h)` 만 원본 표기라 유지
- 방벽 = `__tests__/docs.attendanceHolidayMark.test.ts`(12건)

### ★근본 원인 3가지 — 같은 클래스가 반복됐다
- **소비처 0인 죽은 경로**: 워커가 입력한 측정시간은 `TraineeLogTask.difficulty`에 저장되면서도
  **화면 4곳에서만 읽히고 PDF payload는 한 곳도 읽지 않았다.** 4를 넣어도 문서엔 5.5가 찍혔다.
- **생성 전용 경로를 '재작성'으로 오인**: `bulk-generate`는 기존 행을 건너뛸 뿐 지우지 않고,
  **`DailyAttendance` 삭제 경로가 리포 전역에 0개**라 화면에서 되돌릴 방법이 없었다.
- **기본값이 사용자 선택을 가장**: `Number(taskScore) || 3`이 미선택을 3(보통)으로 굳혀
  실제 '보통' 선택과 구분 불가. 그래서 **소급 전환도 불가능**하다(신규분부터 적용).

### ★★두 개의 시간 축 분리 (사용자 확정)
- `measHours` **직무지도원 관점** = 전일 8 · AM/PM **5.5**(지도 인정)/4.5 → **출근부·급여**. 절대불변 유지
- `traineeMeasHours` **장애인 관점**(신설) = 전일 8 · AM/PM **4.5** → **일지 기본값**
- 일지에 실제로 찍히는 값은 **워커 입력값**이며 위는 미입력 시 기본값. **출근부는 미변경**
- 방벽 = `__tests__/docs.traineeMeasuredTime.test.ts`(15건). 상세=메모리 `work_hours_rules_2026_06_18`

검증: tsc 0 · **vitest 462** · build 0 · 휴무일 dev 통합 **18/18**(잔여 0) ·
드로우 전후 대조 **531케이스**(507 동일·24건 2p→1p·페이지 증가 0) · PDF 스윕 519 악화 0 ·
파일럿 PDF 165 악화 0 · 화면 검증(체크박스 기본 해제·토글·측정시간 4.5) ·
운영 마이그 적용(드리프트 0·컬럼 `nullable=YES`·기존 행 무변경) · 운영 배포 별칭 확인.

**파일럿 일지 1건 삭제 완료**(2026-08-20 곽은하→최승원). 출근기록 14건은 유지 — 새 규칙으로 재작성 가능.
★운영에서 신규 문자열 기능 확인은 **미실시**(무인증 307) — 파일럿 진행 중 실화면 확인 필요.

### 신규 검증 자산
- `scripts/verify-attendance-holiday.mts` — 휴무일 제외 14항목(양성대조·근무인정일 유지·배정격리·행 무손실)

---

## 0. 2026-08-11 종결 (근거 포함)

### 커밋 4건 — 전부 push·배포 완료
| 커밋 | 내용 |
|---|---|
| `5569fe7` | 날짜 왕복검증 3곳(`isValidYmd`) · B-4 `document-versions` 페이로드 경량화 · E-4/E-5 경합 응답코드 |
| `751d83f` | E-2 근로계약 발행 배정 단위 advisory 락 |
| `b7afc56` | B-2 admin/manager 로그인 계정 축 레이트리밋 |
| `5caf204` | Vercel CLI 56.4.1 핀 + 배포 문서 정정 |

검증: tsc 0 · vitest 416 · production build 0 · **dev 스모크 전량** · **운영 실측**.

### ★유령 TODO 정리 — 실측으로 종결
- **C-1 Upstash** — `UPSTASH_REDIS_REST_URL`/`_TOKEN`이 **Preview+Production에 76일 전부터 설정**돼 있었다.
  "몇 주째 최우선 보안 위험"은 실측하지 않아 생긴 유령이었다. **B-2 운영 실증까지 완료**
  (통제 요청 1회 → `rl:count:admin-login-acct:…` 값 `1`·TTL 763s 확인, Redis 실패 로그 없음).
- **토스페이먼츠** 키 설정됨 · **포트원** 키 없음(취소 결정과 정합)
- **카카오 템플릿 5종** 설정(CONTRACT·CONTRACT_SIGNED·SIGNUP·ASSIGN_CONNECT·SURVEY)
- **dev DB 다운** — 삭제가 아니라 **프리티어 자동 일시정지**였고 복원 후 데이터 보존 확인

### 결정으로 종결
- **B-1** 연차 EXPIRE 재발화 → 현행 유지 · **B-3** gov-status 서명게이트 → 현행 유지
- **E-1** checkQuota 소프트리밋 · **E-3** traineeSiteGuard N+1(D-1에 흡수) → 의도적 유지

---

## A. 급여 — Phase 4 (착수 가능)

- [ ] **월급제(MONTHLY) 휴일근로** — `lib/payroll/computeRun.ts:373-379`가 공휴일 출근일을 일할 분자에서 제외,
  `:386` schedDays 상한 → **가산 0.5배만** 지급. HOURLY/DAILY는 1.5배 정상.
  **노무사 회신 불문 ①(근로대가 100% + 가산 50%)을 기본값으로 구현 확정.**
  `MonthlyHolidayPayPolicy` enum(`WORK_PAY_PLUS_PREMIUM`/`PREMIUM_ONLY`) + 기관별 설정 + 운영자만 변경 +
  사유·적용시작연월 필수 + `PayrollRun` 스냅샷 + 확정분 소급 금지. **`proRateDaySet` 변경 금지**(별도 급여라인 신설).

---

## B. D-1 문서 흐름 — ✅**종결(2026-08-12)**

D-1 v5의 미결 7건은 전부 해소됐고, 별도 D-1 문서 없이 **파일럿 v1.8 §12 1단계 구현으로 소화**됐다
(`073ce08` — `TraineeSupervision` + 기간 불변식 + 훈련생 락 NS=4, 전역 락 순서 확정).
1:多 판정은 2026-06-18 절대불변 규칙(현장 재적 수)을 **그대로 유지**하기로 결정했고,
`TraineeSupervision`은 **접근권·coverage 전용**이다(급여 기준 변경의 근거가 아님).

- [ ] 이관: 공단 등록 인원 **중복 식별 방식**(주민번호 전체 수집은 기본안 아님) — 파일럿 참여자 실등록 전 결정
- [ ] 이관: 급여 divergence 분석 · 노무/공단 서식 해석 · 소급 재계산 → **파일럿 2 / 정식 운영 전 과제**
- [ ] 이관: 불연속 supervision 문서화 정책(v1은 **불연속 생성 금지**가 운영 불변식)

---

## C. 파일럿 — ✅**전량 종결(2026-08-14 구현 · 운영 마이그·배포 완료)**. 남은 것은 실제 수행뿐

★**v1.8(인앱 격리 `PilotSession`)은 2026-08-13에 폐기**됐다. 현행 기준 문서 =
`docs/PILOT_ROLLBACK_INSTRUCTION_2026_08_13.md`(**v5**). 파일럿 = 실운영 前 소수 직무지도원 사용성 테스트이며,
**기존 운영 환경에서 전용 자원을 전량 새로 만들고 끝나면 레지스트리 기준으로 전량 삭제**한다.

| 단계 | 커밋 | 상태 |
|---|---|---|
| 1 되돌림 | `2303d7b` | ✅ |
| 2 레지스트리(`Pilot`·`PilotResource`) | `b866841`·`dc803b2` | ✅ |
| 3 운영자 일괄 설정 | `9cf0b57`·`1841901` | ✅ |
| 4 파일럿 문서 경로(수기 공란) | `141b6f1`·`fd6157c`·`a85040c` | ✅ |
| 5 초기화 | `17f9c66`(지시서 v5)·`6b7b218`(5-A)·`8f7ca69`(5-B)·`db59f2a`·`ff1328c`·`14f35d7`·`3061873`(리뷰 반영)·`91c7117`(3단계 분리)·`600926b` | ✅ 화면·HTTP 검증 포함 |

★**초기화는 3단계 분리 구조**다(§10-0): **종료·계정 회수 → 배출 대기 15분 → 전체 삭제**.
단계는 `Pilot.quiescedAt` 으로 복원한다(`READY → DRAINING → PURGE_READY → COMPLETED` / 실패 `RETRY_PENDING`).

검증: tsc 0 · vitest 436 · build 0 · **초기화 110/110**(HTTP 경계 포함, `PILOT_SMOKE_BASE_URL` 없이 실행하면 103) ·
레지스트리 49/49 · 문서 36/36 · PDF baseline 악화 0 · 시각검증 통과 · dev 잔여 0.

- [x] **운영 마이그레이션 적용** — `20260813061045_add_pilot_registry` + `20260814044300_add_pilot_quiesced_at`
      (+`20260812120000_add_trainee_supervision`). `migrate status` up to date · `migrate diff --exit-code` **0**
- [x] **배포 게이트** — 팀 플랜 **hobby** · 프로젝트 `defaultMaxDuration` 미설정 → 서명 업로드 3라우트는 **수십 초**에
      종료(배출 대기 15분보다 훨씬 짧다). 그 과정에서 초기화 라우트 `maxDuration` 300이 상한 초과임을 발견해
      **60으로 조정**(`90a1117`). ★플랜/프로젝트 상한을 올리면 이 값도 같이 올릴 것
- [x] **운영 배포** — `npx vercel@56.4.1 --prod --yes` 수동(master push는 자동배포 없음). `able-link.co.kr` 실측 200
- [x] 운영 DB 셋업 — 파일럿 #1 `2026년 8월_9월 파일럿_곽은하` 생성됨(워커 1·훈련생 4·출근기록 14)
- [ ] **실제 파일럿 수행**(운영 액션, 코드 작업 없음) — `/admin/pilots` 세팅 → `/pilot/docs` 안내 → 의견 청취
      → **종료·계정 회수 → 배출 대기 15분 → 전체 삭제**
- [x] ★**남은 일지 축 정리 — 종결(2026-08-23)**. 배정#1(곽은하·동아ST)을 ADAPTATION 으로 바꾸면서
      그 전에 쓴 일지#2(`trainingType=FIELD`)가 적응지도일지 PDF 에 안 나오는 상태였다.
      **파일럿 일지를 전량 삭제**해 해소(일지 1건·과제행 1건, 잔여 0 / **출근기록 14건은 유지**).
      배정이 이미 ADAPTATION 이라 앞으로 쓰는 일지는 `trainingType=ADAPTATION` 으로 정상 저장된다.
      ★교훈: **배정의 serviceStep 을 바꾸면 그 전에 쓴 일지의 축은 따라오지 않는다** — 일지가 있는
      상태로 단계를 바꾸면 반드시 축을 함께 정리할 것.

상세·재개 절차·실측 함정 = 지시서 §10(초기화 설계) · `docs/WORK_STATUS_2026_08_13_PILOT.md`(v1.8 시절 기록)

---

## D. 위생 — Phase 6

- [ ] **D-3** json 무가드 → 500 **51파일**. `readJsonObject(req)` 공용 헬퍼 도입 후 공개→워커→관리자 순 점진
- [ ] **E-6** `site_holiday_requests(holiday_id) WHERE status='PENDING'` partial unique (기존 중복 검사 선행)
- [ ] 설문 `formSnapshot` 2단계 분리 — ★`app/admin/survey-requests/page.tsx:176`이 `.categories`를 직접 사용해 바로 빼면 회귀
- [ ] `worker/holidays` **레거시 잘못된 날짜 행 일회성 정리** 후 DELETE도 엄격화(현재 의도적 관대)
- [ ] `deployment.md` §CLI 버전 핀 문장 단순화 — "저장값이 원인이면 56.4.1도 실패했어야" 는 엄밀하지 않음
- [ ] `jose` 6.2.3 — 변경내역·보안권고 확인 후 **독립 작업**(6.1.3 유지 중)
- [ ] Vercel CLI 핀 해제 — 후속 버전으로 `npm run deploy:preview` **성공 확인 후에만** 상향
- [ ] **`/worker/docs` 인라인 미리보기가 CSP `object-src 'none'`에 차단**(`page.tsx:571`의 `<object data:…>`).
      서버 로그 `[csp-report]` 실측. `/worker/docs/view`(iframe+same-origin)는 정상이라 기능 자체는 대체돼 있다.
      CSP enforce 전환(`721423e`, 07-14) 이후 계속 죽어 있던 것으로 추정 — 2026-08-12 발견
- [ ] **`admin/workers/invite/route.ts:33-35` 주석 허위** — "소비측도 checkSiteCapacity 이중방어"라 주장하나
      소비측(`worker/invite`) 호출 0건. 주석 정정 또는 실제 이중방어 추가
- [ ] **비파일럿 PDF 담당자명 불일치** — `preview`는 매니저 displayName, `generate`는 빈 값.
      7단계에서 파일럿만 단일 함수로 통일했고 비파일럿은 그대로 둠
- [ ] `.mts` 스크립트에서 `lib/*.ts` named import 런타임 실패(`Object.keys(ns)=['default']`) — 리포 전역 조건.
      기존 `scripts/test-marketplace-rework.mts`도 동일 형태
- [ ] **`worker/signature/route.ts:135-141` `deleteFromStorage`가 응답 `ok`를 검사하지 않는다** — Storage 삭제
      실패가 조용히 성공 처리돼 서명(개인정보)이 버킷에 잔존한다. 같은 파일 `:90`의 DB 갱신 실패 시 방금 올린
      객체의 보상 삭제도 없다(`sign/[token]:121-129`는 같은 상황을 보상 삭제한다 — 두 라우트의 규율이 갈린다).
      파일럿 초기화는 자체 삭제 함수로 우회했다(2026-08-14 발견)
- [ ] **`/admin/pilots/[pilotId]` 배정 기간 표시가 하루 밀린다** — `startDate.slice(0,10)`이 UTC ISO를 자르는데
      저장값은 KST 자정(`2026-08-01T00:00+09:00` = `2026-07-31T15:00Z`)이라 `2026-07-31`로 보인다.
      DB 값은 정확하고 문서 payload도 영향 없다 — **화면 표시만**. 파일럿 전용 화면(2026-08-14 시각검증에서 발견)
- [ ] **`app/worker/docs/page.tsx:606` ESLint `react/no-unescaped-entities` 2건** — `'위탁기관에 최종 제출'`의
      작은따옴표를 `&apos;`로. `8da3c30` 이전부터 있던 레거시이며 파일럿 원복 커밋에서 diff 0 유지를 위해
      `--no-verify`로 우회했다(2026-08-13). **독립적인 운영 코드 정리 작업에서 수정할 것**

---

## E. 횡단

- [ ] **직무지도원 모바일 가독성 점검** — 줄바꿈·폰트. puppeteer/playwright 미설치 → Chrome 연동.
  대상: 로그인 → 홈/출퇴근 → 캘린더 → 일지 → 문서/서명 → 급여 → 프로필

---

## F. 인프라·사업 (코드 아님)

- [ ] ⚠️**Upstash dev/prod 분리** — 로컬 `.env`가 **운영과 동일 인스턴스임을 확정**(운영에 만든 키를 로컬 자격증명으로 조회됨).
  로컬 테스트가 운영 카운터를 오염시키고 **실계정을 30분 잠글 수 있다.**
  인스턴스 분리 + 키 prefix 환경명(`prod:`/`dev:`). staging 구축과 함께
- [ ] staging 환경 · 스토리지 dev/prod 격리
- [ ] 토스페이먼츠 심사 — **9월 결정 예정**(2달 지연 통보)
- [ ] 카카오 비밀번호 재설정 템플릿 — **선택**(주 동선이 매니저 화면표시(무료), 미설정 시 안전 폴백)
- [ ] `vercel/vercel` 이슈 제보 — `--auto-detect root-directory`가 레거시 값에 no-op (선택)

---

## G. 백로그 (의도적 보류)

대시보드 Phase3 · 급여명세서 고도화 · 2층 타입드헬퍼 · 급여 계산 큐(경보 시) ·
cron 교차월 캐치업 · HOURLY payLines 반올림 드리프트 ·
manager-signup/holiday 경합(E-4·E-5는 409로 정리 완료) · 직종 확대(별도 트랙)

---

## 권장 순서 (2026-08-22 갱신)

1. **파일럿 실제 수행** — 코드 작업 없음. 진행 중 나오는 문서 결함은 08-22처럼 즉시 반영
   (★파일럿에서 나온 결함이라도 **운영 전반의 결함이면 운영 코드로 고친다** — 08-22 4건이 그 사례)
2. **Phase 4 A** 월급제 휴일근로
3. Phase 6 위생 · 모바일 가독성
4. Upstash 분리는 staging과 함께
5. 파일럿 2 / 정식 운영 전 과제(급여 divergence·공단 서식 해석)는 파일럿 1 운영 결과를 보고

### ★파일럿 중 재발 가능성이 높은 것 (08-22 조사에서 드러난 인접 결함)
- **`DailyAttendance` 삭제 경로가 리포 전역에 0개** — 잘못 만든 출근부를 화면에서 지울 수 없다.
  이번엔 문서 표시 제외로 해결했지만, 행 자체를 지워야 하는 요구가 오면 신규 기능이 필요하다.
- **`bulk-generate`는 '재작성'이 아니다** — 이미 있는 날짜는 건너뛴다. 버튼 문구가 오해를 부를 수 있다.
